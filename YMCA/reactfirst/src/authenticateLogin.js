export function authenticateLogin(email, password) {
    console.log("hello!");
    // fetch email here
    
    // if the email is on file, proceed
    //   fetch the password CRYPTOGRAPHICALLY
    //   if the actual password does not match input password,
    //      return an error and let user know

    return false;
}